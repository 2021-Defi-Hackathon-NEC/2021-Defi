<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.wallet_bottom_nav_2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Wallet_bottomnav2">

        <activity
            android:name=".auth_pincode"
            android:theme="@style/PinTheme">

        </activity>
        <activity android:name=".login_1">

        </activity>
        <activity android:name=".login_2">

        </activity>
        <activity android:name=".before_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity> <!-- <activity -->
        <!-- android:name=".ui.login.LoginActivity" -->
        <!-- android:label="@string/title_activity_login"></activity> -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <activity android:name=".ui.wallet.BALWalletFragment" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />
    </application>

</manifest>